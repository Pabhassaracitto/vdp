targets:
  $default:
    builders:
      freezed:freezed:
        generate_for:
          - lib/models/*.dart  # Chỉ quét các file trong thư mục này
      source_gen:combining_builder:
        generate_for:
          - lib/models/*.dart